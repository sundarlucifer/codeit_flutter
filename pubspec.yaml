name: codeit
description: All in one app for Codeit

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2

  flutter_bloc: ^3.2.0
  
  firebase_core: ^0.4.4+3
  firebase_auth: ^0.15.5+3
  google_sign_in: ^4.3.0

  flutter_svg: ^0.17.3+1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/images/
